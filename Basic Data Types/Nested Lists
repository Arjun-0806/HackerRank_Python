if __name__ == '__main__':
    l=[]
    for _ in range(int(input())):
        name=input()
        marks=float(input())
        l.append([name,marks])
sh=sorted(set([marks for name,marks in l]))[1]
print('\n'.join(sorted([name for name,marks in l if marks==sh])))
